{
  "name": "TippmixApp Dev Environment",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "postCreateCommand": "flutter doctor && flutter pub get && corepack enable && corepack prepare pnpm@10.15.1 --activate && pnpm install --filter ./cloud_functions",
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.flutter",
        "esbenp.prettier-vscode",
        "ms-python.python"
      ]
    }
  },
  "remoteUser": "vscode"
}
