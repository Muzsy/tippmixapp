#!/usr/bin/env bash
# Placeholder commands if flutter_launcher_icons / flutter_native_splash are configured in pubspec.yaml
# Update pubspec.yaml accordingly before running.

set -euo pipefail

# Generate launcher icons
# flutter pub run flutter_launcher_icons:main

# Generate native splash
# flutter pub run flutter_native_splash:create

echo "Update pubspec.yaml with launcher icon and splash config, then run the above commands."
